import{_ as e,p as o,q as c,a1 as a}from"./framework-1bc831b6.js";const d={},t=a(`<h1 id="全局对象" tabindex="-1"><a class="header-anchor" href="#全局对象" aria-hidden="true">#</a> 全局对象</h1><div class="custom-container tip"><p class="custom-container-title">TIP</p><p>在查看此部分内容前，你应当已经阅读文档中的“进阶”部分</p></div><p>全局对象是 MeloBot 中可供全局使用的一个字典。其主要包含了所有配置文件中的配置项和一些运行时的数据和信息。</p><p>使用以下方法即可导入全局对象 <code>BOT_STORE</code>:</p><div class="language-python line-numbers-mode" data-ext="py"><pre class="language-python"><code><span class="token keyword">from</span> utils<span class="token punctuation">.</span>globalData <span class="token keyword">import</span> BOT_STORE
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>其中，<code>BOT_STORE</code> 的结构如下：</p><ul><li><code>operation</code>：该键名下存储配置文件的 <code>operation</code> 子键</li><li><code>custom</code>：该键名下存储配置文件的 <code>custom</code> 子键</li><li><code>cmd</code>：该键名下存储配置文件的 <code>cmd</code> 子键</li><li><code>kernel</code>：该键名下存储一些重要的运行时信息（需要使用请自行查阅 <code>./utils/globalData.py</code>）</li><li><code>data</code>：bot 启动后存储到全局的数据（目前只包含关键词应答规则字典，在该键下的 <code>KEY_ANS</code> 子键）</li></ul><p>同时，如果命令模板中命令执行时，需要保留某种“会话”状态，建议在 <code>BOT_STORE[&#39;data&#39;]</code> 新建一个不重名键。每次进入命令执行时，读取判断即可。</p>`,8),l=[t];function s(n,i){return o(),c("div",null,l)}const r=e(d,[["render",s],["__file","global.html.vue"]]);export{r as default};
